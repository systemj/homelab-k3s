apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mcpo-time.fullname" . }}-test-connection"
  labels:
    {{- include "mcpo-time.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mcpo-time.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
