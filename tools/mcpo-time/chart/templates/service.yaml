apiVersion: v1
kind: Service
metadata:
  name: {{ include "mcpo-time.fullname" . }}
  labels:
    {{- include "mcpo-time.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mcpo-time.selectorLabels" . | nindent 4 }}
