apiVersion: v1
kind: Service
metadata:
  name: {{ include "kiwix-serve.fullname" . }}
  labels:
    {{- include "kiwix-serve.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kiwix-serve.selectorLabels" . | nindent 4 }}
