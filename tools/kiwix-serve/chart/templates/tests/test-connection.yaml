apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kiwix-serve.fullname" . }}-test-connection"
  labels:
    {{- include "kiwix-serve.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kiwix-serve.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
